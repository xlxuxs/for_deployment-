# Civic Engagement Web Dashboard

Fresh React dashboard for planner and admin users.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

The app uses `https://citizenvoice-backend.onrender.com/api` by default.
